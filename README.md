# assignment7-1
# ReadMe:  From Terminal
  # 1.1	a database name testdb created with userid ‘amar’ and password ‘123456789’
	# 1.2	a table named ‘student’ created with field id as int, name as text and age as int
	# 1.3	sql used to insert,view,update and delete the data.
	# 1.4	sql used to truncate the table content and delete the table.
# ReadMe: Python
  # 2.1	getConnection function created to check the connection
	# 2.2	create_table() function created to create table named employee
	# 2.3	the result is checked in schema of postgresql server desktop admin application 	         pgadmin4. The screen shot is attached.
	# 2.4	insert_data() function is created to insert data to the table. The inserted data was 	checked in table testdb schema in pgadmin4 application.
	# 2.5	delete_data() function is to deleted a selected record by an id
	# 2.6	show_data() function is to show the data by selecting id & all
# Submitted, please.
